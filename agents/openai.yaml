interface:
  display_name: "Source-Aligned Change Plan Coverage"
  short_description: "从 source 建立 v7 Capability、交付顺序与完整 evidence handoff"
  default_prompt: "使用 $source-aligned-change-plan-coverage 从指定 source document 建立 v7 JSON initial framework、Capability-first 且 sequence-independent 的 OpenSpec Change 全局计划，并完成 evidence coverage、final roadmap、terminal mapping 与 integration-passed handoff。"
